version: '3.8'

services:
  cv-workspace:
    build: .
    container_name: wood_cv_env
    ports:
      - "8888:8888"
      - "8080:8080"
    volumes:
      - .:/workspace
    command: jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --allow-root
